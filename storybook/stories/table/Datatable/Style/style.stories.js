import React from "react"

import { StoryPage } from "@components"

import { style } from "./style"

export default {
  title: "Components / Table / DataTable",
}

export const Style = context => <StoryPage {...style} {...context} />
